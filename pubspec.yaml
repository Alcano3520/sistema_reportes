name: sistema_reportes
description: "Sistema de reportes para recursos humanos"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 🔥 Dependencias principales
  supabase_flutter: ^2.5.6
  provider: ^6.1.2
  uuid: ^4.4.0
  
  # 🎨 UI y iconos
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# ✅ ASSETS COMENTADOS TEMPORALMENTE
# flutter:
#   uses-material-design: true
#   assets:
#     - assets/images/
#     - assets/icons/

flutter:
  uses-material-design: true